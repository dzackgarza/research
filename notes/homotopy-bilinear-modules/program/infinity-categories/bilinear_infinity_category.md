<!-- Absorbed from the Coxeter research corpus on 2026-08-20 under
     PLAN-coxeter-deletion-audit-registry (reader H). The body below is the
     source document, unchanged. -->

> **Origin.** `Coxeter/research/explorations/connections/homotopy_theory/infinity_categories/bilinear_infinity_category.md`
>
> **Preamble status.** Absent. The preamble owns homsets of formed modules as first-class parents (`FormHomset` in `form_modules.sage`), which is the object this document would enrich to a mapping space; nothing above that exists.
>
> **Recorded error.** The `ModelCategoryStructure` block asserts the projective model structure on chain complexes of formed modules and the standard t-structure with heart the formed-module category. Both statements presuppose that the category is abelian. See INDEX.md: with form-preserving morphisms it is not even additive, so neither the model structure nor the t-structure is defined as stated.

---

# Interface: ∞-Category of Bilinear Modules

The ∞-categorical enhancement of BilR-Mod, encoding all higher homotopical information.

## Basic ∞-Category Structure

```python
class BilinearModuleInfinityCategory:
    r"""
    The ∞-category BilR-Mod^∞ of bilinear modules.
    
    This enhances the ordinary category BilR-Mod with:
    - Higher morphisms (2-morphisms are homotopies, 3-morphisms are homotopies between homotopies, etc.)
    - Homotopy coherent compositions
    - ∞-categorical limits and colimits
    
    Modeled as a quasi-category (simplicial set satisfying inner horn filling).
    """
    
    def __init__(self, ring):
        """
        Initialize the ∞-category of bilinear modules over a ring.
        
        INPUT:
        - ring: Base ring R
        """
        self.ring = ring
        self._setup_simplicial_structure()
    
    def mapping_space(self, M, N):
        r"""
        The mapping space Map(M, N) as a simplicial set/Kan complex.
        
        - 0-simplices: Morphisms M → N preserving bilinear forms
        - 1-simplices: Homotopies between morphisms
        - n-simplices: Higher homotopies
        
        OUTPUT:
        Kan complex (∞-groupoid) of maps
        
        EXAMPLES::
        
            sage: M = BilinearModule(matrix(ZZ, [[2]]))
            sage: N = BilinearModule(matrix(ZZ, [[4]]))
            sage: Map_MN = BilRMod_infinity.mapping_space(M, N)
            sage: Map_MN.pi_0()  # Connected components = morphisms up to homotopy
            Hom(M, N) / homotopy
        """
        pass
    
    def homotopy_category(self):
        r"""
        The homotopy category Ho(BilR-Mod^∞).
        
        This is the ordinary category obtained by:
        - Objects: Same as BilR-Mod^∞
        - Morphisms: π_0 of mapping spaces
        
        For discrete rings, this recovers the ordinary category BilR-Mod.
        """
        pass
```

## Higher Limits and Colimits

```python
class InfinityCategoricalLimits:
    r"""
    Homotopy coherent limits and colimits in BilR-Mod^∞.
    """
    
    @staticmethod
    def homotopy_pullback(f, g):
        r"""
        The homotopy pullback (homotopy fiber product) of f: A → C and g: B → C.
        
        This is the ∞-categorical limit of the diagram A → C ← B.
        
        Explicitly: P = {(a, b, h) | a ∈ A, b ∈ B, h: f(a) ~ g(b) in C}
        
        EXAMPLES::
        
            sage: # Homotopy pullback of the inclusions Z → Q and Z → Z[1/2]
            sage: A = BilinearModule(ZZ, [[1]])
            sage: B = BilinearModule(QQ, [[1]])
            sage: C = BilinearModule(ZZ.localize(2), [[1]])
            sage: P = homotopy_pullback(A.to(B), A.to(C))
            sage: # P captures the "intersection" with coherence data
        """
        pass
    
    @staticmethod
    def homotopy_pushout(f, g):
        r"""
        The homotopy pushout (homotopy colimit) of f: C → A and g: C → B.
        
        Dual to homotopy pullback.
        """
        pass
    
    @staticmethod
    def homotopy_limit(diagram):
        r"""
        General homotopy limit over an ∞-category-valued diagram.
        
        INPUT:
        - diagram: Functor from index ∞-category to BilR-Mod^∞
        
        OUTPUT:
        The ∞-categorical limit with universal property
        """
        pass
```

## Presentable ∞-Category Structure

```python
class PresentableStructure:
    r"""
    BilR-Mod^∞ as a presentable ∞-category.
    
    Key properties:
    - Has all small ∞-categorical limits and colimits
    - Accessible (generated by κ-compact objects)
    - Related to R-Mod^∞ by forgetful/free adjunction
    """
    
    def compact_objects(self):
        r"""
        The subcategory of compact objects in BilR-Mod^∞.
        
        Over Noetherian rings, these are the finitely presented bilinear modules.
        """
        pass
    
    def ind_completion(self):
        r"""
        BilR-Mod^∞ as Ind-completion of finitely presented bilinear modules.
        
        Every object is a filtered colimit of compact objects.
        """
        pass
    
    def localizing_subcategories(self):
        r"""
        Classification of localizing subcategories.
        
        These correspond to subsets of Spec(R) by the Hopkins-Neeman theorem
        (when applicable).
        """
        pass
```

## Monoidal ∞-Category Structure

```python
class MonoidalInfinityStructure:
    r"""
    The symmetric monoidal ∞-category structure on BilR-Mod^∞.
    """
    
    def tensor_product_derived(self, M, N):
        r"""
        The derived tensor product M ⊗^L N in the ∞-category.
        
        This is computed by:
        1. Resolve M or N by cofibrant replacement
        2. Take ordinary tensor product
        3. Result is independent of choices up to equivalence
        """
        pass
    
    def internal_hom_derived(self, M, N):
        r"""
        The derived internal hom RHom(M, N).
        
        Satisfies tensor-hom adjunction in the ∞-category:
        Map(M ⊗^L N, P) ≃ Map(M, RHom(N, P))
        """
        pass
    
    def dualizable_objects(self):
        r"""
        Objects with duals in the symmetric monoidal ∞-category.
        
        M is dualizable if there exists M^∨ with:
        - Evaluation: M ⊗ M^∨ → 1
        - Coevaluation: 1 → M^∨ ⊗ M
        - Triangle identities up to coherent homotopy
        """
        pass
```

## Model Category Presentation

```python
class ModelCategoryStructure:
    r"""
    Model category structure on chain complexes of bilinear modules.
    
    This presents the ∞-category BilR-Mod^∞.
    """
    
    def __init__(self):
        """
        The projective model structure on Ch(BilR-Mod).
        
        - Weak equivalences: Quasi-isomorphisms
        - Fibrations: Epimorphisms in positive degrees
        - Cofibrations: Generated by inclusions of subcomplexes
        """
        self.weak_equiv_class = "quasi-isomorphisms"
        self.fibration_class = "positive-epimorphisms"
        self.cofibration_class = "subcomplex-inclusions"
    
    def cofibrant_replacement(self, M):
        r"""
        Replace M by a cofibrant complex quasi-isomorphic to M.
        
        Typically use projective resolution.
        """
        pass
    
    def fibrant_replacement(self, M):
        r"""
        Replace M by a fibrant complex (all complexes are fibrant here).
        """
        return M  # All objects fibrant in projective model structure
    
    def homotopy_category_from_model(self):
        r"""
        Ho(Ch(BilR-Mod)) ≃ D(BilR-Mod) ≃ Ho(BilR-Mod^∞).
        
        The homotopy category of the model category is the derived category.
        """
        pass
```

## t-Structures and Hearts

```python
class tStructure:
    r"""
    t-structures on the ∞-category BilR-Mod^∞.
    """
    
    def standard_t_structure(self):
        r"""
        The standard t-structure with heart equivalent to BilR-Mod.
        
        - D^≤0: Complexes with H^i = 0 for i > 0
        - D^≥0: Complexes with H^i = 0 for i < 0
        - Heart: D^≤0 ∩ D^≥0 ≃ BilR-Mod
        """
        pass
    
    def perverse_t_structure(self, stratification):
        r"""
        Perverse t-structures for stratified bilinear modules.
        
        Useful for studying singularities in the moduli of bilinear forms.
        """
        pass
    
    def truncation_functors(self):
        r"""
        Truncation functors τ^≤n, τ^≥n associated to t-structure.
        
        These give the "intelligent truncation" that preserves homotopical info.
        """
        pass
```

## Stability Conditions

```python
class StabilityConditions:
    r"""
    Bridgeland stability conditions on D^b(BilR-Mod).
    
    A stability condition consists of:
    - Central charge Z: K_0(BilR-Mod) → C
    - Heart of bounded t-structure
    - Compatibility conditions
    """
    
    def stability_manifold(self):
        r"""
        The space Stab(BilR-Mod) of stability conditions.
        
        This is a complex manifold that encodes:
        - Wall-crossing phenomena
        - Moduli of stable objects
        - Connections to mirror symmetry
        """
        pass
    
    def stable_objects(self, stability_condition):
        r"""
        The abelian category of semistable objects for given stability.
        """
        pass
```