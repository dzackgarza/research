[pytest]
markers =
    tdd_red: the assertion states an interface obligation the repository does not yet meet; the conftest hook fails such a test if it passes, so a satisfied obligation cannot go unnoticed
