# Category Specs Purge Audit

This report records the salvage decision for the rejected `origin/main` tail after
`9769adf2`. The acceptance standard is source-grounded mathematical/API
correctness, not downstream ledger reduction.

Accepted salvage is in `1440c8ff`. The old rejected history remains reachable at
`safety/pre-purge-20260525-category-spec-ledger-gaming`.

The post-salvage report refresh records `ordinary_error_count: 1717` and preserves
the 370-row `sage-stubs` external subset. Treat that ledger as diagnostic evidence
of the accepted source state, not as completion of `sage-stubs` issue #5.

## Accepted Source-Grounded Hunks

| Source commits | Kept in `1440c8ff` | Source basis |
| --- | --- | --- |
| `0e7bd2a` | `PolynomialRings.ParentMethods.completion` imports and passes `infinity` instead of the prelude-only `oo` spelling. | Sage source defines `infinity` in `sage/rings/infinity.py:1730`; polynomial-ring completion accepts infinite precision in `sage/rings/polynomial/polynomial_ring.py:620`. |
| `00af6a4`, `b2c42c7` | Set-family, finite-set, finite-countable, recursive-enumerated, and image-subobject Python protocol methods use `int` for `__len__`/`__hash__`. | Python data model and Sage implementations at `finite_enumerated_sets.py:83`, `family.pyx:753`, `image_set.py:197`, and `recursively_enumerated_set.pyx:550`. |
| `f119142` | Finite-rank free modules restate finite rank as `Integer`, dual as `DualModule`, and use the rank only at Python `range` boundaries. Ordered-basis order routes through the existing `basis_index_set()` helper. | `FiniteRankFreeModule.rank`, `dual`, and finite-rank tensor methods are public Sage methods at `finite_rank_free_module.py:622`, `2675`, `1618`, `2369`, `2759`, `2797`, and `2873`. |
| `ab24a59` | Integral-lattice `LLL` is the no-argument, lattice-returning surface, kept as an abstract spec obligation rather than replaying the rejected final Sage-specific body. The `short_vectors_up_to_sign` call uses a precise Sage lattice cast instead of `Any`. | Sage `FreeQuadraticModule_integer_symmetric.LLL` is no-argument and returns a sublattice at `free_quadratic_module_integer_symmetric.py:1472`. |

## Replayed As Aligned

| Source commit | Local replacement | Disposition |
| --- | --- | --- |
| `4f1b1da` | `dbcc98bb` | Kept. It rejects fake `Morphism.is_invertible` stubbing and keeps invertibility on the local End/Aut mathematical surface. |

## Per-Hunk Source Audit Progress

The audit below is the post-purge hunk-level salvage pass. It is not a replay of the
old commit units.

| Hunk family | Old diff inspected | Current salvage state | Disposition |
| --- | --- | --- | --- |
| Formed-module and bilinear-form subcategory decorator removals | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/forms/subcategories` | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/forms/subcategories` is empty. | Reject all hunks in this family. The old tail only removed `@override` and/or `@final` from predicate witnesses and inherited formed-module method surfaces in `alternating.py`, `bilinear.py`, `definite.py`, `free_bilinear.py`, `indefinite.py`, `integral.py`, `nondegenerate.py`, `quadratic.py`, `rational.py`, `symmetric.py`, and `with_forms.py`. No hunk changes a mathematical owner, codomain, or implementation in a source-grounded way. The controlling sources are `category_specs/AGENTS.md`, `category-spec-style`, `SPEC-MAPPING-FORMS.md`, and `SPEC-MAPPING-MODULES.md`: forms owns `FormedModules(R)`, bilinear/quadratic axioms, form-preserving Hom/Aut surfaces, and integral/nondegenerate/symmetric refinements. Removing decorators to make downstream override/finality diagnostics disappear weakens obligation evidence without recording a replacement owner. |
| RationalField singleton and public-method overrides | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/rings/subcategories/rational_field.py` | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/rings/subcategories/rational_field.py` is empty. | Reject all hunks in this file. The `Rings().Characteristic(Integer(0))` rewrite is a local scalar-coercion/type-checker accommodation; existing category specs use `Rings().Characteristic(0)` for characteristic-zero singleton chains, and the style rule permits Sage-style literal scalar notation. The remaining hunks only remove `@override` from `QQ` method surfaces such as `algebraic_closure`, number-field delegation methods, embeddings, units, orders, and discriminants. Sage source exposes real `RationalField` public methods in `sage/rings/rational_field.py`, and `sage-stubs` PR #6 names RationalField as a remaining issue-5 family. If these methods are missing from static sidecars, the aligned fix is in `sage-stubs`; deleting consumer override markers is reclassification-only closure. |
| `SageCategory` return/cast and category-selector weakening in algebras, topological spaces, and posets | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/algebras category_specs/topological_spaces category_specs/posets`; focused with `rg -n "SageCategory|cast\\(|super_categories|extra_super_categories|from sage.categories.category import Category"` over the old diff. | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/algebras category_specs/topological_spaces category_specs/posets` is empty. | Reject the inspected `SageCategory` return/cast hunks and the coupled selector-finality and poset-broadening hunks. The old tail changes `super_categories`, `extra_super_categories`, `_with_axiom`, and subcategory selectors from project `Category` surfaces to raw Sage-category annotations, adds local casts around category selectors, removes some `@final` selector markers, and broadens poset element/subset surfaces from `PosetElement` or `PosetSubset` to `CategoryElement | Hashable` and plain lists. The controlling sources are `category-spec-style` (casts and type signatures are proof obligations; checker-model gaps belong to stub/plugin/QC lanes), `category-spec-subtrees` and `SPEC-MAPPING-CAT.md` (construction selectors return project category objects), and `category_specs/topological_spaces/category_obligations.sage` (the topological root has no direct Sage supercategory splice). Sage inventories justify preserving mapped Sage category edges, not replacing the local mathematical category surface with `SageCategory` annotations or casts. |
| Module constructor cast layer, literal `SageInteger` rewrites, decorator removals, and subobject method deletions | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/modules`; focused with `rg -n "^diff|^@@|^[+-].*(@final|@override|cast\\(|def |SageInteger|SageParent|SageRing|FreeModule|VectorSpace|submodule|quotient_module|__and__)"` over the old diff. | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/modules` contains only the already accepted finite-rank and ordered-basis hunks in `subcategories/free.py` and `subcategories/with_basis.py`. The old `modules/__init__.py` cast layer and related decorator/deletion hunks are absent. | Reject the module constructor/interoperability hunks not already listed under accepted source-grounded salvage. The old tail adds `_sage_ring`/`_sage_field`/`_sage_commutative_ring` helpers, imports Sage implementation container types, recasts `FreeModule`, `VectorSpace`, `span`, matrix, finite-rank, ideal, and invariant-factor constructor inputs through Sage-specific containers, rewrites mathematical integer literals such as `2`, `1`, and `len(zs)` to `SageInteger(...)`, removes `@final`/`@override` evidence from tensor, constructor, selector, and predicate methods, and deletes subobject conveniences such as `__and__`, `__le__`, and no-argument `quotient_module`. The controlling spec `SPEC-MAPPING-MODULES` says constructor signatures expose named mathematical input cases rather than Sage positional dispatch, integer-valued module data uses `Integer`, type vocabulary should name mathematical objects rather than implementation containers, raw Sage option plumbing is rejected, and construction categories/codomain data do not move caller ownership. Sage source confirms the public surfaces (`Modules.super_categories`, `tensor_square`, `quotient`, `FreeModule`, `VectorSpace`, `span`, and `FiniteRankFreeModule`) but does not justify local Sage-container cast waves or deleting mathematical obligation markers. |
| Residual ring singleton, precision, valuation, algebraic-field, and default-literal hunks | `git diff --no-ext-diff --unified=60 9769adf2..origin/main -- category_specs/rings/__init__.py category_specs/rings/subcategories/algebraic_closure_of_rational_field.py category_specs/rings/subcategories/algebraic_field.py category_specs/rings/subcategories/complete_discrete_valuation_field.py category_specs/rings/subcategories/complex_precision_field.py category_specs/rings/subcategories/dedekind_domain.py category_specs/rings/subcategories/field.py category_specs/rings/subcategories/integer_ring.py category_specs/rings/subcategories/local_field.py category_specs/rings/subcategories/p_adic_ring.py category_specs/rings/subcategories/real_algebraic_field.py category_specs/rings/subcategories/real_precision_field.py`, plus targeted `rg` for the changed methods in local specs and installed Sage. | `git diff --no-ext-diff --unified=0 9769adf2..HEAD -- category_specs/rings` contains only the accepted polynomial-completion `infinity` spelling. No residual singleton/default/decorator hunk is salvaged. | Reject these residual ring hunks. They rewrite scalar defaults such as `0` and `1` to `Integer(0)`/`Integer(1)` in ideal generators, integer-mod-ring constructors, cyclotomic fields, algebraic polynomial roots, characteristic/Krull-dimension singleton categories, p-adic Frobenius, and series-ring generators. Sage sources overwhelmingly expose these defaults as ordinary Python integer literals (`gen(n=0)`, `polynomial_root(..., multiplicity=1)`, `frobenius_endomorphism(n=1)`), while `SPEC-MAPPING-RINGS` says dimensions, indices, precisions, orders, degrees, and multiplicities are Sage integer quantities typed as `Integer`; it does not require constructor-call defaults in the spec surface. The remaining hunks remove `@override` from ring predicate or precision methods such as `is_complete_discrete_valuation_field`, `is_local_field`, and `change_precision` even though `category_specs/rings/__init__.py` or the approximate-ring category declares the inherited surface. This loses obligation evidence without a replacement owner. Any source-backed refinement of those concrete ring families should be admitted through the rings mapping/constructor specs, not by type-checker-driven literal rewrites or decorator deletion. |
| Commutative-ring extension receiver casts and polynomial/series extension signature rewrites | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/rings/subcategories/commutative.py category_specs/rings/subcategories/polynomial_ring.py category_specs/rings/subcategories/power_series_ring.py category_specs/rings/subcategories/laurent_series_ring.py category_specs/rings/subcategories/puiseux_series_ring.py`; checked against `sage-category-source-maps` `ring-integration.md`, `SPEC-MAPPING-RINGS`, and installed Sage `rings/ring.pyx`, `polynomial_ring.py`, `power_series_ring.py`, `laurent_series_ring.py`, and `puiseux_series_ring.py`. | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/rings/subcategories/commutative.py category_specs/rings/subcategories/polynomial_ring.py category_specs/rings/subcategories/power_series_ring.py category_specs/rings/subcategories/laurent_series_ring.py category_specs/rings/subcategories/puiseux_series_ring.py` contains only the accepted `oo` to `infinity` polynomial-completion spelling fix. | Reject the remaining old hunks in this family. They replace the explicit commutative-ring `extension` surface with a broad `**kwds` union, change polynomial/series `extension` parameters from ring-element and ring-morphism data to `Polynomial` plus generic `CategoryObject`, add receiver casts to `CommutativeRing`, locally reject `latex_names` and `map=True` with `NotImplementedError`, remove override/finality evidence, and rewrite `gen(n: Integer = 0)` defaults as `Integer(0)`. Sage source confirms `Ring.extension(poly, name=None, names=None, **kwds)`, number-field and rational-field extension keyword routes, polynomial `completion(p, prec)` with infinite precision producing a lazy power-series ring, and series/polynomial `gen(n=0)`. The rings mapping requires named, documented constructor/input shapes, explicit admitted options rather than unresolved option bags, `Integer` as the mathematical surface type while allowing constructor bodies to pass to Sage factories, and no raw variadic plumbing. Therefore only the `infinity` import/call spelling is accepted; receiver casts, option suppression, broad keyword typing, and literal `Integer(0)` rewrites are rejected as local static-model appeasement or hidden surface narrowing. |
| Set protocol-return salvage versus set-surface weakening | `git diff --no-ext-diff 9769adf2..origin/main -- category_specs/sets`; focused with `rg -n "^diff|^@@|^[+-].*(@override|@final|def |__len__|__hash__|_element_constructor_|_an_element_|SageCategory|cast\\(|object|Cardinality|SetElement|lift|retract|_sympy_|super_categories)"` over the old diff. | `git diff --no-ext-diff 9769adf2..HEAD -- category_specs/sets` contains only `__len__ -> int` in `countable.py`, `finite.py`, `family.py`, and `recursively_enumerated.py`, plus `ImageSubobject.__hash__ -> int` in `image.py`. | Keep the current Python protocol return salvages and reject the rest of the old set-family hunks. Python and Sage sources justify `__len__` and `__hash__` returning native `int`; Sage `FiniteEnumeratedSets.ParentMethods.__len__`, finite `Family.__len__`, `ImageSubobject.__hash__`, and `RecursivelyEnumeratedSet.__len__` implement Python protocol methods. The rejected old hunks add `SageCategory` return casts to set-category edges, widen `_element_constructor_` and rank/indexing inputs to `object`, `SageCoercionAtom`, native `int`, or slice/list unions, narrow `cardinality()` from mathematical `Cardinality` to `int`, remove `@final` and `@override` obligation evidence, and delete required `ImageSubobject.lift`, `retract`, and `_sympy_` obligations. `SPEC-MAPPING-SETS` keeps `ImageSubobject` as a subobject/subquotient with `ambient`, `lift`, and `retract`, keeps `cardinality` as set cardinality, treats `_sympy_` as compatibility behavior where Sage supplies it rather than a reason to delete inherited obligations, and rejects raw implementation containers, option bags, and category-obligation-driven weakening. Red flag log: old hunks contain repeated `cast(...)` around category/sage-parent boundaries and `isinstance(el, Integer)` in `integer_range.py`; only the latter is boundary-justified constructor coercion, while the repeated category casts and widened object/Sage-atom signatures are design-smell/QC-appeasement and rejected. |
| Hom/Aut and lattice Hom/constructor surfaces | `git diff --no-ext-diff --unified=0 9769adf2..origin/main -- category_specs/homsets category_specs/lattices category_specs/types.py`; checked against `SPEC-MAPPING-HOMSETS`, `SPEC-MAPPING-LATTICES`, `SPEC-20260504-FORMS-ISOMETRY-HOM-CONTAINMENT-OWNER`, `SPEC-20260504-FORMS-SYMMETRIC-BILINEAR-DIVISIBILITY-OWNER`, `category-framework-design` Hom/Aut references, `lattice-redesign` references, and installed Sage `free_quadratic_module_integer_symmetric.py`. | `git diff --no-ext-diff --unified=0 9769adf2..HEAD -- category_specs/homsets category_specs/lattices category_specs/types.py` contains the aligned Hom/Aut helper signature and invalid `@override` removal from `dbcc98bb`, plus the cleaned integral-lattice `short_vectors_up_to_sign` cast and abstract no-argument `LLL -> Lattice` salvage. It does not contain the old lattice Hom inheritance rewrite, lattice construction `SageCategory` annotation churn, over-Dedekind rank broadening, or `types.py` alias opacity. | Keep the generic Hom/Aut ownership hunks from `4f1b1da`: `_is_invertible_endomorphism` depends on the project `UniversalEndElementMethods` surface that actually declares `is_invertible`, and `HomCategoryConstruction.default_super_categories` is a class-level construction hook rather than an override of a visible instance method. Keep only the cleaned integer-lattice salvage already recorded above: Sage `FreeQuadraticModule_integer_symmetric.LLL(self)` is no-argument and returns a lattice object; `short_vectors_up_to_sign` may cast precisely to the Sage backend at that interop boundary. Reject the old lattice Hom rewrite from `ab24a59`: replacing `FreeBilinearModulesCategory.HomCategory.ElementMethods` with `UniversalHomElementMethods`, dropping inherited endomorphism/morphism bases, and removing the `is_isometry` override erases the formed-module Hom-containment owner that the specs require. Reject the lattice construction hunks that import raw `sage.categories.category.Category`, annotate `extra_super_categories`/`super_categories` with Sage `Category`, and add local `Ring` variables only to placate static typing; the accepted design keeps project category objects and routes checker-model gaps to stub/plugin/QC lanes. Reject `fbffa3` over-Dedekind changes: a finitely generated torsion-free module over a Dedekind domain has a finite constant projective rank, not generic cardinality, and deleting `is_over_dedekind_domain()` removes a defining predicate witness. Reject the coupled `types.py` changes not already accepted through other rows: `CommutativeRing`, `Cardinality = int | SageElement`, and `SetElement = SageElement` are opacity/broadening moves tied to rejected cast and signature churn, not independent mathematical improvements. |

## Source Coverage Checkpoint

The inspected source range is exactly:

- `git diff --name-only 9769adf2..origin/main -- category_specs | sort | wc -l` -> 99 source files.
- `git diff --stat 9769adf2..origin/main -- category_specs` -> 99 files, 630 insertions, 549 deletions.
- `git diff --stat 9769adf2..HEAD -- category_specs` -> 11 files, 32 insertions, 37 deletions.

Those 99 files are partitioned by the hunk-family rows above:
forms subcategories; RationalField; algebras/topological-spaces/posets
`SageCategory` and selector hunks; module constructor/provider hunks; residual rings;
commutative/polynomial/series extension hunks; sets; Hom/Aut/lattices; and coupled
`types.py` aliases. The current 11-file source delta is the accepted salvage surface:
generic Hom/Aut ownership, integral-lattice `LLL` and short-vector backend cast,
finite-rank/ordered-basis module corrections, polynomial-completion `infinity`, and
Python protocol return fixes in set subcategories.

## Rejected Commits

| Commit | Disposition |
| --- | --- |
| `eaebf6b` | Rejected. Only removes `@override` from the finite-rank issue-5 family; the source-backed fix belongs in `sage-stubs`. |
| `f015de4` | Rejected report refresh based on rejected finite-rank consumer edits. |
| `0e7bd2a` | Partly mined for `infinity`; override-deletion hunks remain rejected. |
| `64767af` | Rejected. It replaces missing-sidecar pressure with local casts and consumer-side module/countable routing. |
| `00af6a4` | Partly mined for finite-countable `__len__`; override-deletion hunks remain rejected. |
| `2ca825` | Rejected report refresh based on rejected ownership edits. |
| `2ec5341` | Rejected. It removes override evidence and adds an interop cast, without a mathematical owner change. |
| `ccbde8c` | Rejected. It removes module predicate override obligations. |
| `29eb986` | Rejected. RationalField public methods are real Sage surfaces; deleting local override markers is not stub completion. |
| `eeb9dfe` | Rejected report refresh based on rejected ownership edits. |
| `267dd6` | Rejected report refresh; downstream evidence must follow accepted source or stub changes. |
| `fc527e` | Rejected report refresh; downstream evidence must follow accepted source or stub changes. |
| `01190d` | Rejected report refresh; downstream evidence must follow accepted source or stub changes. |
| `f70e2b` | Rejected report refresh; downstream evidence must follow accepted source or stub changes. |
| `384ba0` | Rejected report refresh; downstream evidence must follow accepted source or stub changes. |
| `a2f35e` | Rejected. It handles the named commutative-ring extension and polynomial completion pressure by consumer receiver casts, broad keyword forwarding, and local NotImplemented guards. |
| `9adf1c` | Rejected. It removes `@final` from set providers as a QC accommodation without a source-grounded replacement owner. |
| `b2c42c` | Partly mined for Python dunder protocol returns; `Cardinality`/`SetElement` ontology rewrites remain rejected. |
| `a9cffd` | Rejected. It relaxes formed-module finality instead of recording a source-grounded extension-hook owner. |
| `9b1400` | Rejected. Set-enumeration signature broadening and `SageCategory` return changes are static-model/interop appeasement, not accepted spec design. |
| `46cf7e` | Rejected. It widens `_element_constructor_` to `object` and deletes image subobject obligations for `lift`, `retract`, and `_sympy_`. |
| `0fb6a7` | Rejected. Topological `SageCategory`/cast changes are a static-model lane, not a mathematical spec correction. |
| `6d67ac` | Rejected. Algebra `SageCategory`/cast return changes are a static-model lane, not a mathematical spec correction. |
| `fbffa3` | Rejected. Over-Dedekind rank broadening and predicate-provider deletion weaken the lattice surface. |
| `df5e3b` | Rejected. Poset category return changes and selector finality changes are static-model appeasement. |
| `ab24a5` | Partly mined for integral-lattice LLL/precise cast; lattice Hom base rewrites, Poset broadening, finality removals, and report refreshes remain rejected. |
| `f11914` | Partly mined for finite-rank and ordered-basis corrections; submodule operation deletions, generic free-rank narrowing, and report-count framing remain rejected. |
| `142f8c` | Rejected report refresh based on rejected reductions. |
| `ee9ad7` | Rejected. It adds a broad local cast layer in module constructors to appease current stubs. |
| `1e28e1` | Rejected. Literal `0`/`1` defaults are allowed Sage-style notation; `Integer(0)` rewrites were type appeasement. |
| `7fb186` | Rejected report refresh based on rejected constructor/default changes. |
| `556c6d` | Rejected. Set-leaf cast waves, `SageCategory` return changes, and broad constructor/input domains need a separate source-map decision rather than PR #6 credit. |
| `583980` | Rejected report refresh based on rejected set-leaf reductions. |

## Boundary Ruling

The purge is not a claim that the rejected commits contain no true statements. It
is a claim that their original commit units are not acceptable evidence for
`sage-stubs` issue #5 or category-spec completion. Future mining must enter as a
new source-grounded commit like `1440c8ff`, not by replaying the rejected commits.
