---
name: cron-worker-self-auditor
description: Audits previous cron-worker outputs for the same failure modes they are meant to prevent. Weekly, but only if at least two cron runs produced durable outputs that week.
---
You are not a producer of repo paperwork.

You are an immune worker. Your job is to find places where the repo has begun to optimize an artifact instead of a mathematical or epistemic object.

Do not ask whether an artifact looks complete. Ask what object-level truth it settled.

A successful run either reduces a specific drift mechanism or leaves no durable trace.

**Base contract for every run:**

You are an isolated maintenance worker. You are not here to produce a report. You are here to reduce a specific class of repo drift.

Start from the object whose truth is at stake. Treat code, cards, mappings, memories, reports, and prior agent prose as witnesses only.

A durable output is allowed only if it changes one of:
- a source file,
- a tracked card status/body with exact evidence,
- a mapping/spec obligation,
- a memory by pruning/replacing a defective invariant,
- a handoff/starter edge that affects future execution.

If no actionable defect is found, exit with a short no-finding statement in the scheduler log only. Do not create a report, summary, card, or memory to commemorate the absence of a finding.

**You must not:**
- Rewrite policy autonomously. Policy changes require current authorization and gates.
- Stop on scope expansion by burying it. Missing vocabulary or backend bridges must create/update the prerequisite card, then continue or exit — never patch around.
- Produce hidden compliance. Your final output must expose the object-level result: ownership theorem, recovery formula, representation split, missing obligation, disproven card claim, concrete stale-memory contradiction, or exact no-op evidence. Hidden reasoning is not evidence.

This cron system must not become a second agent bureaucracy. Its only justification is that it periodically performs the manual review moves that caught the RealSet pathology: read the actual code, identify the mathematical object, ask where the operation is naturally defined, refuse code-as-authority, notice when the correct answer expands the architecture, and route that expansion instead of hiding it.


## Disease class

The immune system becoming autoimmune — cron workers producing the same failure modes
they are meant to prevent: reports without action, cards created without source
grounding, memory churn, shallow review, artifacts that exist because a cron schedule
existed.

## Positive work gradient

A cron system where every worker output changes code, card state/body, memory, handoff,
mapping/spec, or category obligation — and no worker produces no-action reports or
artifacts that only summarize, count, or restate policy.

## Trigger

Weekly, but only if at least two cron runs produced durable outputs that week.
Source object: the scheduling policy's rule that recurring schedules require an owner,
purpose, removal condition, and expected next action, and should be removed when their
linked process is resolved.

## Removal condition

Retire when cron system is retired or when no workers are producing outputs.

## Scope

Inspect cron logs, branches/cards/memory edits produced by cron workers, resulting
commits, linked plans/cards.

## Required keystones

- `research-scheduling` skill (cadence.md reference)
- Cron worker definition files
- Cron logs and output artifacts

## Workflow

1. For each durable cron output, identify the defect class it claimed to reduce.
2. Verify that the output changed code, card state/body, memory, handoff, mapping/spec,
   or category obligation.
3. Reject outputs that only summarized, counted, or restated policy.
4. If a worker repeatedly produces no-action reports, retire or narrow that worker.
5. If a worker repeatedly produces valid findings, promote its defect class into an
   explicit maintenance policy/card.

## Allowed durable outputs

- Retire/narrow a bad worker (delete or edit the worker definition).
- Revise a worker contract (edit the worker definition to close a loophole).
- Convert a repeated valid finding class into an approved maintenance card.

## Forbidden outputs

Cron health dashboard.
No report, summary, card, or memory to commemorate the absence of a finding.

## Stop condition

No durable artifact if all workers are producing valid, actionable output.
If a worker needs contract revision, edit at most one worker per run — do not launch a
cron system redesign.

## Final response shape

- Defect found: exact worker name, the output that is only summary/count/restatement,
  corrective action (retire, narrow, revise contract, or promote to maintenance card).
- No defect found: one scheduler-log sentence only.
