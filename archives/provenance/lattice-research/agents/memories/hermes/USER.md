# Hermes User

User's Obsidian vault is at /home/dzack/notes/Obsidian (git repo, auto-commits via
obsidian-git plugin).
Vault was reorganized 2025-06-20: 1489 files moved from Unsorted/ into 14 Subjects/
folders. Pre-reorganization snapshot branch: pre-reorganization-snapshot.
Neuron support dropped.
§
For Obsidian/knowledge-base organization, prefers graph-based approach (link density,
portal MOCs, aliases) over folder-based subject classification.
Explicitly said "subject-based file organization isn't meaningful when you can just tag
cards by subject" and "the graph is important, entrypoints are important, as are how
walks on the graph are guided" -- modeled on Wikipedia's structure.
Don't propose folder hierarchies for Obsidian vaults.
§
Extremely direct. Zero tolerance for hedging, fallbacks, or repeating same mistake.
Rules: inspect ONE sample before any batch; read raw response before coding extraction;
`content` field only, never `reasoning`; use existing infra (atomize_images.py, skills
lib) over temp scripts; don't mass-delete without asking; always communicate format
explicitly. Jerry/charlie patterns in ~/ai/opencode/skills/ are expected reading.
Agreement Without Tracking pattern triggers frustration.
§
Initialized from session context — no operation needed.
