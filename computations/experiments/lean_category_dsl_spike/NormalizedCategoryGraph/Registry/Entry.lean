/-
Copyright (c) 2026 Dzack Garza. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
-/
import NormalizedCategoryGraph.Core.Expr

/-!
# Registry entries

Declaration names are stored as Lean `Name` values.  JSON serialization is a
presentation concern; registration and environment lookup retain the checked
identity.
-/

namespace NormalizedCategoryGraph

/-- Named category registry row. -/
structure NamedCategoryEntry where
  id : CategoryId
  canonicalName : String
  declaration : Lean.Name
  expression : CategoryExpr
  /-- Optional elaborated witness tying this expression to the declared category. -/
  realization : Option Lean.Name := none
  origin : CategoryOrigin
  visibility : Visibility
  deriving Repr, Inhabited

/-- The sort of an explicitly bound category-family parameter. -/
inductive CategoryFamilyParameterKind
  | ringObject
  deriving DecidableEq, Repr, Inhabited

/-- Declared variance of a category family.  This does not assert a registered action on morphisms. -/
inductive CategoryFamilyVariance
  | restrictionOfScalarsContravariant
  deriving DecidableEq, Repr, Inhabited

/-- A bound parameter of a category family. -/
structure CategoryFamilyParameter where
  name : String
  kind : CategoryFamilyParameterKind
  deriving Repr, Inhabited

/--
A parameterized category family, distinct from any selected category node.

`fibreDeclaration` names the Lean declaration that accepts the bound parameter
and returns the corresponding category.  The registry records transport
orientation separately: a family value does not assert covariance.
-/
structure CategoryFamilyEntry where
  id : CategoryFamilyId
  canonicalName : String
  declaration : Lean.Name
  parameter : CategoryFamilyParameter
  fibreDeclaration : Lean.Name
  variance : CategoryFamilyVariance
  deriving Repr, Inhabited

/-- Classifier registry row. -/
structure ClassifierEntry where
  id : ClassifierId
  canonicalName : String
  declaration : Lean.Name
  hostId : CategoryId
  visibility : Visibility
  deriving Repr, Inhabited

/-- Semantic role of a registered functor. -/
inductive FunctorRole
  | generatedStructural
  | opaqueStructuralPort
  | theoremBacked
  | finiteLimit
  | constructorAction
  | presentationOnly
  deriving DecidableEq, Repr, Inhabited

/-- Whether a functor may participate in automatic structural projection. -/
inductive StructuralAdmissibility
  | generated
  | declared
  | excluded
  deriving DecidableEq, Repr, Inhabited

/-- A typed functor declaration, with expression endpoints checked by Lean. -/
structure FunctorEntry where
  id : FunctorId
  canonicalName : String
  source : CategoryExpr
  target : CategoryExpr
  declaration : Lean.Name
  expression : FunctorExpr source target
  role : FunctorRole
  admissibility : StructuralAdmissibility
  port : Option PortId
  origin : String
  coherenceClass : Option CoherenceId
  preferredPresentation : Bool
  deriving Repr

/-- A categorical constructor declaration. -/
structure ConstructorEntry where
  id : ConstructorId
  canonicalName : String
  source : CategoryExpr
  target : CategoryExpr
  declaration : Lean.Name
  sourcePosition : String
  deriving Repr

/-- A named finite-limit cone with an elaborated witness declaration. -/
structure FiniteLimitConeEntry where
  id : ConeCertificateId
  source : CategoryExpr
  target : CategoryExpr
  apex : CategoryExpr
  declaration : Lean.Name
  sourcePosition : String
  deriving Repr

/-- A coherence witness between two elaborated functor declarations. -/
structure CoherenceEntry where
  id : CoherenceId
  source : FunctorId
  target : FunctorId
  declaration : Lean.Name
  sourcePosition : String
  deriving Repr

/-- A theorem-backed inclusion that is not generated by refinement. -/
structure TheoremInclusionEntry where
  id : StructuralTheoremId
  source : CategoryExpr
  target : CategoryExpr
  declaration : Lean.Name
  sourcePosition : String
  deriving Repr

/-- Spelling alias — does not create a semantic node. -/
structure AliasEntry where
  id : AliasId
  spelling : String
  aliasOf : CategoryId
  declaration : Lean.Name
  deriving Repr, Inhabited

/-- Opaque category with typed structural ports. -/
structure StructuralPortEntry where
  id : OpaquePortId
  source : CategoryId
  target : CategoryId
  role : PortId
  declaration : Lean.Name
  provenance : String
  deriving Repr, Inhabited

structure OpaqueCategoryEntry where
  id : CategoryId
  declaration : Lean.Name
  ports : Array StructuralPortEntry
  reason : String
  visibility : Visibility
  deriving Repr, Inhabited

/-- Alternative presentation equivalence (not an alias). -/
structure PresentationEquivalenceEntry where
  source : CategoryId
  target : CategoryId
  declaration : Lean.Name
  deriving Repr, Inhabited

/-- Curated presentation metadata; it does not alter semantic registry entries. -/
structure PresentationMetadataEntry where
  id : PresentationId
  category : CategoryId
  declaration : Lean.Name
  visibility : Visibility
  sourcePosition : String
  deriving Repr

/-- Aggregate registry snapshot for export (specimen / compiled). -/
structure RegistrySnapshot where
  schemaVersion : String
  categories : Array NamedCategoryEntry
  categoryFamilies : Array CategoryFamilyEntry
  classifiers : Array ClassifierEntry
  functors : Array FunctorEntry
  constructors : Array ConstructorEntry
  finiteLimitCones : Array FiniteLimitConeEntry
  coherences : Array CoherenceEntry
  theoremInclusions : Array TheoremInclusionEntry
  aliases : Array AliasEntry
  opaqueCategories : Array OpaqueCategoryEntry
  equivalences : Array PresentationEquivalenceEntry
  presentations : Array PresentationMetadataEntry
  deriving Repr, Inhabited

end NormalizedCategoryGraph
