# Inherit the repo-root environment (SAGE_BIN, PYTHONPATH via RESEARCH_EXPERIMENTS).
source_up
# No bytecode litter: the root `just test` hygiene sweep rejects __pycache__.
export PYTHONDONTWRITEBYTECODE=1
