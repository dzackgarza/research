# Experiments

Runnable exploratory code that is not yet reusable enough for `components/`.

Do not force experiments into object-taxonomy folders such as curves, surfaces, or
lattices. Use a direct file, a dated scratch name, or a named computation thread that
matches the actual calculation.

Promote stable code to `components/` only after preserving the original algorithm and
adding verification appropriate to the mathematical claim.
