<!--
Origin: gitclones/integral_lattice/docs/checklist.md
Copied 2026-08-20 by the integral_lattice enrichment migration
(PLAN-corpora-audit-registry, section R3). Content below is unmodified.
-->

# Oscar Integral Lattice Integration Checklist

Use this checklist to track which Oscar.jl lattice APIs have been wrapped or validated in Sage.

For any questions regarding these methods, refer to:

- The docs: https://docs.oscar-system.org/v1/Hecke/manual/quad_forms/integer_lattices/
  - Note that EVERY method has a direct link to the source on Github.
- The source code: https://github.com/thofma/Hecke.jl/blob/v0.38.5/src/QuadForm/Quad/ZLattices.jl

## ZZLat Constructors & Named Lattices

- [x] `integer_lattice([B::MatElem]; gram)` – general constructor from basis and optional Gram matrix (IntegralLattice dispatch in integral_lattice/constructors.py:120).
- [x] `lattice(V::AbstractSpace, basis::MatElem; check::Bool=true)` – embed a basis inside an ambient quadratic space (IntegralLattice/\_make_lattice with `basis` in integral_lattice/constructors.py:65).
- [x] `root_lattice(R::Symbol, n::Int)` – ADE/I root lattice factory (IntegralLattice string constructor in integral_lattice/constructors.py:285).
- [x] `hyperbolic_plane_lattice(n::RationalUnion = 1)` – hyperbolic plane scaled by `n` (IntegralLattice string constructor for `U` and `H` in integral_lattice/constructors.py:285).
- [x] `integer_lattice(S::Symbol, n::RationalUnion = 1)` – symbolic shortcuts (`:H`, `:U`, `:Z`) (IntegralLattice string constructor in integral_lattice/constructors.py:285).
- [x] `leech_lattice()` – standard Leech lattice constructor.
- [x] `leech_lattice(niemeier_lattice::ZZLat)` – Leech lattice via Niemeier neighbour data.
- [x] `k3_lattice()` – Beauville–Bogomolov–Fujiki K3 lattice.
- [x] `mukai_lattice(S::Symbol = :K3; extended::Bool=false)` – Mukai lattices for hyperkähler types.
- [x] `hyperkaehler_lattice(S::Symbol; n::Int = 2)` – BBF lattices for hyperkähler manifolds.
- [x] `rescale(L::ZZLat, r::RationalUnion)` – rescale quadratic form by rational factor (FreeQuadraticModule_integer_symmetric.twist in integral_lattice/FreeModule/FreeBilinearModule/Lattice/lattice_other.py:202).

## ZZLat Basic Accessors & Structure

- [x] `ambient_space(L::AbstractLat)` – retrieve ambient quadratic space (FreeBilinearModule.ambient_space in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py).
- [x] `basis_matrix(L::ZZLat)` – current basis matrix (FreeModule.basis_matrix in integral_lattice/FreeModule/free_module.py).
- [x] `gram_matrix(L::ZZLat)` – Gram matrix of the current basis (FreeBilinearModule.gram_matrix in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:41).
- [x] `rational_span(L::ZZLat)` – ambient quad space generated by the lattice (FreeModule.rational_span in integral_lattice/FreeModule/free_module.py).

## ZZLat Arithmetic Invariants

- [x] `rank(L::AbstractLat)` – rank of the Z-module (FreeModule.rank in integral_lattice/FreeModule/free_module.py:133).
- [x] `det(L::ZZLat)` – determinant of Gram matrix (FreeBilinearModule.det in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:90).
- [x] `scale(L::ZZLat)` – ideal generated by inner products (FreeBilinearModule.scale in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:101).
- [x] `norm(L::ZZLat)` – ideal generated by norms (FreeBilinearModule.norm in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:105).
- [x] `is_even(L::ZZLat)` – evenness test (BilinearZModule.is_even in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:78).
- [x] `is_integral(L::AbstractLat)` – integrality test (FreeBilinearModule.is_integral in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:91).
- [x] `is_primary_with_prime(L::ZZLat)` – detect primary lattices (BilinearZModule.is_primary_with_prime in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:238).
- [x] `is_primary(L::ZZLat, p)` – test `p`-primary status (BilinearZModule.is_primary in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:241).
- [x] `is_elementary_with_prime(L::ZZLat)` – detect elementary lattices (BilinearZModule.is_elementary_with_prime in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:244).
- [x] `is_elementary(L::ZZLat, p)` – test `p`-elementary status (BilinearZModule.is_elementary in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:247).
- [x] `genus(L::ZZLat)` – Conway–Sloane genus symbol.
- [x] `mass(L::ZZLat)` – mass of the genus.
- [x] `genus_representatives(L::ZZLat)` – representatives of isometry classes in the genus.
- [x] `signature_tuple(L::ZZLat)` – real signature triple (BilinearZModule.signature_tuple in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:85).
- [x] `is_positive_definite(L::AbstractLat)` – positive definiteness test (BilinearZModule.is_positive_definite in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:97).
- [x] `is_negative_definite(L::AbstractLat)` – negative definiteness test (BilinearZModule.is_negative_definite in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:104).
- [x] `is_definite(L::AbstractLat)` – definiteness test (BilinearZModule.is_definite in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:111).

## ZZLat Isometry & Automorphism Tools

- [x] ⚠️ `automorphism_group_generators(L::AbstractLat; ambient_representation=true, depth=-1, bacher_depth=0)` – generators for `O(L)` (INDEF_FORM_AutomorphismGroup in integral_lattice/lattice_algorithms.py:118; Oscar doc assumes L is definite).
- [x] ⚠️ `automorphism_group_order(L::AbstractLat; depth=-1, bacher_depth=0)` – order of `O(L)` (Oscar doc assumes L is definite).
- [x] ⚠️ `is_isometric(L::AbstractLat, M::AbstractLat; depth=-1, bacher_depth=0)` – global isometry test (INDEF_FORM_TestEquivalence in integral_lattice/lattice_algorithms.py:132; Oscar doc assumes lattices are definite).
- [x] `is_locally_isometric(L::ZZLat, M::ZZLat, p::Int)` – local isometry at `p` (Lattice.is_locally_isometric in integral_lattice/FreeModule/FreeBilinearModule/Lattice/lattice.py:33).

## Root Lattice Utilities

- [x] ⚠️ `root_lattice_recognition(L::ZZLat)` – identify ADE components (Oscar doc requires L to be definite).
- [x] ⚠️ `root_lattice_recognition_fundamental(L::ZZLat)` – ADE components with fundamental root bases (Oscar doc requires L to be definite).
- [x] `ADE_type(G::MatrixElem)` – ADE type from Gram matrix (lattice_utils.py).
- [x] `coxeter_number(ADE::Symbol, n)` – Coxeter number (lattice_utils.py).
- [x] `highest_root(ADE::Symbol, n)` – highest root coordinates (lattice_utils.py).

## ZZLat Module Operations & Order Relations

- [x] `is_sublattice(L::AbstractLat, M::AbstractLat)` – sublattice test (FreeModule.is_submodule in integral_lattice/FreeModule/free_module.py:162).
- [x] `is_sublattice_with_relations(M::ZZLat, N::ZZLat)` – sublattice with transition matrix (Submodule.is_submodule in integral_lattice/FreeModule/submodule.py).
- [x] `+(L::AbstractLat, M::AbstractLat)` – lattice sum in common ambient space (FreeModule.**add** in integral_lattice/FreeModule/free_module.py:166).
- [x] `*(a::RationalUnion, L::ZZLat)` – scalar dilation (FreeBilinearModule.**rmul** delegating to twist in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:94).
- [x] `intersect(L::AbstractLat, M::AbstractLat)` – intersection in common ambient space (module-level logic in integral_lattice/FreeModule/submodule.py:69 with bilinear lift in integral_lattice/FreeModule/FreeBilinearModule/bilinear_submodule.py:12).
- [x] `Base.in(v::Vector, L::ZZLat)` – membership test for single vector. _Stubbed via identity-basis integrality check in_ `integral_lattice/FreeModule/free_module.py`.
- [x] `Base.in(v::QQMatrix, L::ZZLat)` – membership test for row span. _Stubbed via row-wise integrality check assuming identity basis in_ `integral_lattice/FreeModule/free_module.py`.
- [x] `primitive_closure(M::ZZLat, N::ZZLat)` – primitive closure in `M`.
- [x] `is_primitive(M::ZZLat, N::ZZLat)` – test primitive sublattice inclusion. _Handled by_ `FreeModule.is_primitive_submodule` _delegating to_ `Submodule.is_primitive` _in_ `integral_lattice/FreeModule/free_module.py` _and_ `integral_lattice/FreeModule/submodule.py`.
- [x] `is_primitive(L::ZZLat, v::Union{Vector, QQMatrix})` – vector primitiveness. _Constructs the span in_ `FreeModule.is_primitive_submodule` _and reuses the same invariant-factor criterion._
- [x] `divisibility(L::ZZLat, v::Union{Vector, QQMatrix})` – divisibility ideal (BilinearZModule.div in integral_lattice/FreeModule/FreeBilinearModule/bilinear_z_module.py:232).
- [x] `orthogonal_submodule(L::ZZLat, S::ZZLat)` – orthogonal complement submodule (FreeBilinearModule.orthogonal_complement in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:118).
- [x] ⚠️ `irreducible_components(L::ZZLat)` – definite lattice decomposition (DefiniteLattice.irreducible_components in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/DefiniteLattice/definite_lattice.py).
- [x] `dual(L::AbstractLat)` – dual lattice (FreeBilinearModule.dual in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:155).
- [x] `glue_map(L::ZZLat, S::ZZLat, R::ZZLat; check=true)` – primitive extension glue data (Lattice.glue_map in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/lattice.py).
- [x] `overlattice(glue_map::TorQuadModuleMap)` – reconstruct overlattice from glue map (FreeBilinearModule.overlattice in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:195).
- [x] `local_modification(M::ZZLat, L::ZZLat, p)` – adjust local structure at prime (Lattice.local_modification in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/lattice.py).
- [x] `maximal_integral_lattice(L::AbstractLat)` – enlarge to maximal integral overlattice (Lattice.maximal_integral_lattice in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/lattice.py).

## ZZLat Embeddings & Category Operations

- [x] `direct_sum(x::Vararg{ZZLat})` / `direct_sum(::Vector{ZZLat})` – biproduct with injections (FreeModule.direct_sum in integral_lattice/FreeModule/free_module.py:193 and IntegralLatticeDirectSum in integral_lattice/free_quadratic_module_integer_symmetric.py:175).
- [x] `direct_product(x::Vararg{ZZLat})` / `direct_product(::Vector{ZZLat})` – product with projections (aliased to direct_sum).
- [x] `biproduct(x::Vararg{ZZLat})` / `biproduct(::Vector{ZZLat})` – simultaneous sum/product data (aliased to direct_sum).
- [x] `kernel_lattice(L::ZZLat, f::MatElem; ambient_representation=true)` – kernel of an endomorphism (IMPLEMENTED as LatticeMorphism.kernel in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/LatticeHom/lattice_morphism.py:48).
- [x] `invariant_lattice(L::ZZLat, G::Union{Vector{MatElem}, MatElem}; ambient_representation=true)` – fixed lattice under group action (IMPLEMENTED in G_Lattice.invariant_lattice in integral_lattice/FreeModule/G_FreeModule/G_Lattice/g_lattice.py:60).
- [x] `coinvariant_lattice(L::ZZLat, G::Union{Vector{MatElem}, MatElem}; ambient_representation=true)` – orthogonal complement to invariants (IMPLEMENTED in G_Lattice.coinvariant_lattice in integral_lattice/FreeModule/G_FreeModule/G_Lattice/g_lattice.py:81).
- [x] `embed(S::ZZLat, G::Genus; primitive::Bool=true)` – embed into a genus representative (Lattice.embed wrapping Oscar in integral_lattice/FreeModule/FreeBilinearModule/Lattice/lattice.py:106, Genus class available in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/genus.py).
- [x] `embed_in_unimodular(S::ZZLat, pos::Int, neg::Int; primitive=true, even=true)` – embed into an (even) unimodular lattice (Lattice.embed_in_unimodular bridging Oscar in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/lattice.py:257).
- [x] ⚠️ `vectors_of_square_and_divisibility(L::ZZLat, S::ZZLat, n::RationalUnion, d::RationalUnion=scale(L); coordinates_representation=:S, check=true)` – enumerate vectors with given square & divisibility (Oscar doc assumes S is definite). IMPLEMENTED in DefiniteLattice class.
- [x] ⚠️ `vectors_of_square_and_divisibility(L::ZZLat, n::RationalUnion, d::RationalUnion)` – shortcut when `S = L` (Oscar doc assumes L is positive definite). IMPLEMENTED in DefiniteLattice class.
- [x] `enumerate_quadratic_triples(Q::MatrixElem{T}, b::MatrixElem{T}, c::RationalUnion; algorithm=:short_vectors, equal=false)` – affine quadratic inequality solver (DefiniteLattice.enumerate_quadratic_triples in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/DefiniteLattice/definite_lattice.py:128).

## ZZLat Reduction & Vector Enumeration

- [x] `lll(L::ZZLat, same_ambient::Bool=true)` – (indefinite) LLL basis reduction. IMPLEMENTED in DefiniteLattice class.
- [x] ⚠️ `short_vectors(L::ZZLat, [lb=0], ub, [elem_type]; check::Bool=true)` – list short vectors up to sign (Oscar doc requires L to be positive definite). IMPLEMENTED in DefiniteLattice class.
- [x] ⚠️ `short_vectors_iterator(L::ZZLat, [lb=0], ub, [elem_type]; check::Bool=true)` – iterator over short vectors (Oscar doc requires L to be positive definite). IMPLEMENTED in DefiniteLattice class.
- [x] ⚠️ `shortest_vectors(L::ZZLat, [elem_type]; check::Bool=true)` – shortest vectors and norm (Oscar doc requires L to be positive definite). IMPLEMENTED in DefiniteLattice class.
- [x] `minimum(L::ZZLat)` – minimum norm. IMPLEMENTED in DefiniteLattice class.
- [x] `kissing_number(L::ZZLat)` – kissing number of sphere packing. IMPLEMENTED in DefiniteLattice class.

## Matrix Group Actions on ZZLat

- [x] `invariant_lattice(L::ZZLat, G::MatrixGroup)` – lattice fixed by group action (IMPLEMENTED in G_Lattice.invariant_lattice in integral_lattice/FreeModule/G_FreeModule/G_Lattice/g_lattice.py:60).
- [x] `coinvariant_lattice(L::ZZLat, G::MatrixGroup)` – orthogonal complement to invariants (IMPLEMENTED in G_Lattice.coinvariant_lattice in integral_lattice/FreeModule/G_FreeModule/G_Lattice/g_lattice.py:81).
- [x] `invariant_coinvariant_pair(L::ZZLat, G::MatrixGroup)` – invariants and coinvariants together. IMPLEMENTED in G_FreeModule.invariant_coinvariant_pair in integral_lattice/FreeModule/G_FreeModule/g_free_module.py.

## ZZLatWithIsom Constructors & Accessors

- [x] `integer_lattice_with_isometry(L::ZZLat, f::QQMatrix; check::Bool=true, ambient_representation=true)` – build `(L, f)`.
- [ ] `integer_lattice_with_isometry(L::ZZLat)` – auto-detect ambient isometry.
- [ ] `lattice(Vf::QuadSpaceWithIsom)` – integral lattice inside quadratic space with isometry.
- [ ] `lattice(Vf::QuadSpaceWithIsom, B::MatElem)` – specify sublattice basis.
- [ ] `lattice_in_same_ambient_space(Lf::ZZLatWithIsom, B::MatElem)` – reuse ambient context.
- [x] `ambient_isometry(Lf::ZZLatWithIsom)` – ambient isometry.
- [x] `ambient_space(Lf::ZZLatWithIsom)` – ambient quadratic space with isometry.
- [x] `isometry(Lf::ZZLatWithIsom)` – lattice isometry matrix.
- [x] `lattice(Lf::ZZLatWithIsom)` – underlying `ZZLat`.
- [x] `order_of_isometry(Lf::ZZLatWithIsom)` – order of `f`.

## ZZLatWithIsom Invariants & Properties

- [x] `basis_matrix(Lf::ZZLatWithIsom)` – induced basis matrix.
- [x] `characteristic_polynomial(Lf::ZZLatWithIsom)` – characteristic polynomial of `f`.
- [x] `degree(Lf::ZZLatWithIsom)` – lattice degree.
- [x] `det(Lf::ZZLatWithIsom)` – determinant.
- [x] `discriminant(Lf::ZZLatWithIsom)` – discriminant.
- [x] `genus(Lf::ZZLatWithIsom)` – genus of underlying lattice.
- [x] `gram_matrix(Lf::ZZLatWithIsom)` – Gram matrix.
- [x] `is_definite(Lf::ZZLatWithIsom)` – definiteness test.
- [x] `is_even(Lf::ZZLatWithIsom)` – evenness test.
- [x] `is_elementary(Lf::ZZLatWithIsom, p)` / `is_elementary_with_prime(Lf::ZZLatWithIsom)` – elementary tests.
- [x] `is_integral(Lf::ZZLatWithIsom)` – integrality test.
- [x] `is_positive_definite(Lf::ZZLatWithIsom)` – positive definite.
- [x] `is_primary(Lf::ZZLatWithIsom, p)` / `is_primary_with_prime(Lf::ZZLatWithIsom)` – primary tests.
- [x] `is_negative_definite(Lf::ZZLatWithIsom)` – negative definite.
- [x] `is_unimodular(Lf::ZZLatWithIsom)` – unimodularity test.
- [x] `minimum(Lf::ZZLatWithIsom)` – minimum norm.
- [x] `minimal_polynomial(Lf::ZZLatWithIsom)` – minimal polynomial of `f`.
- [x] `norm(Lf::ZZLatWithIsom)` – lattice norm.
- [x] `rank(Lf::ZZLatWithIsom)` – rank.
- [x] `rational_span(Lf::ZZLatWithIsom)` – rational span.
- [x] `scale(Lf::ZZLatWithIsom)` – scale ideal.
- [x] `signature_tuple(Lf::ZZLatWithIsom)` – signature triple.
- [x] `type(Lf::ZZLatWithIsom)` – type of isometry pair.
- [x] `is_of_type(Lf::ZZLatWithIsom, t::Dict)` – match prescribed type data.
- [x] `is_of_same_type(Lf::ZZLatWithIsom, Lg::ZZLatWithIsom)` – compare types.
- [x] `is_of_hermitian_type(Lf::ZZLatWithIsom)` – hermitian-type predicate.
- [x] `is_hermitian(t::Dict)` – dictionary describes hermitian type.
- [x] `hermitian_structure(Lf::ZZLatWithIsom)` – associated hermitian lattice.
- [x] `signatures(Lf::ZZLatWithIsom)` – archimedean signatures for hermitian type.
- [x] `rational_spinor_norm(Lf::ZZLatWithIsom)` – spinor norm of `f`.

## ZZLatWithIsom Operations & Substructures

- [x] `Base.:^(Lf::ZZLatWithIsom, n::Int)` – iterate isometry.
- [x] `direct_sum(::Vector{ZZLatWithIsom})` – orthogonal direct sums with isometry data.
- [ ] `dual(Lf::ZZLatWithIsom)` – dual lattice with induced isometry.
- [ ] `lll(Lf::ZZLatWithIsom)` – LLL reduction respecting isometry.
- [x] `orthogonal_submodule(Lf::ZZLatWithIsom, Q::QQMatrix)` – orthogonal submodule for subspace.
- [x] `rescale(Lf::ZZLatWithIsom, r::RationalUnion)` – rescale pairing.
- [x] `discriminant_group(Lf::ZZLatWithIsom)` – discriminant module plus induced action.
- [x] `coinvariant_lattice(Lf::ZZLatWithIsom)` – coinvariant lattice of `f`.
- [x] `invariant_lattice(Lf::ZZLatWithIsom)` – invariant lattice of `f`.
- [x] `invariant_coinvariant_pair(Lf::ZZLatWithIsom)` – invariants and coinvariants together. IMPLEMENTED via inheritance from G_FreeModule.
- [ ] `kernel_lattice(Lf::ZZLatWithIsom, p::Union{ZZPolyRingElem, QQPolyRingElem})` – kernel of polynomial in `f`.
- [ ] `kernel_lattice(Lf::ZZLatWithIsom, m::Integer)` – kernel of integer multiple of `f`.

## Primitive Embedding & Gluing Toolkit

- [ ] `primitive_embeddings(L::ZZLat, M::ZZLat; classification=:sub, check=true)` – primitive embeddings when source is genus-unique.
- [ ] `primitive_embeddings(G::ZZGenus, M::ZZLat; classification=:sub)` – primitive embeddings into lattices of a genus.
- [ ] `primitive_embeddings(T::TorQuadModule, invariants::Tuple{Int,Int}, M::ZZLat)` – embeddings from torsion quadratic modules with invariants.
- [ ] `primitive_embeddings(q::TorQuadModule, sign::Tuple{Int,Int}, M::ZZLat; classification=:sub)` – embeddings via genus symbol.
- [ ] `primitive_extensions(M::ZZLat, N::ZZLat; glue_order=nothing, q=nothing, even=is_even(M)&&is_even(N), classification=:subsub)` – classify primitive overlattices of direct sums.
- [ ] `equivariant_primitive_extensions(M::Union{ZZLat, ZZLatWithIsom}, N::Union{ZZLat, ZZLatWithIsom}; glue_order=nothing, q=nothing, even=is_even(M)&&is_even(N), classification=:subsub, compute_bar_Gf=true)` – glue with compatible isometries.
- [ ] `admissible_equivariant_primitive_extensions(A::ZZLatWithIsom, B::ZZLatWithIsom, C::ZZLatWithIsom, p::Int, q::Int)` – Nikulin/BH admissible triples algorithm.
- [ ] `admissible_triples(G::ZZGenus, p::Int)` – enumerate `p`-admissible triples of genera.
- [ ] `is_admissible_triple(G1::ZZGenus, G2::ZZGenus, G3::ZZGenus, p::Int)` – test admissibility of triple.

## Discriminant & Group Action Utilities

- [x] `discriminant_group(L::ZZLat)` – discriminant module of integral lattice (FreeBilinearModule.discriminant_group in integral_lattice/FreeModule/FreeBilinearModule/free_bilinear_module.py:90).
- [x] `invariant_coinvariant_pair(L::ZZLatWithIsom)` – (repeat) ensure glue workflow done. IMPLEMENTED via inheritance from G_FreeModule.
- [x] `discriminant_representation(L::ZZLat, G::MatrixGroup)` – group action on discriminant group. IMPLEMENTED in LatticeAutSpace.discriminant_representation in integral_lattice/FreeModule/FreeBilinearModule/BilinearZModule/Lattice/LatticeHom/lattice_autspace.py:150.

## Miscellaneous References

- [ ] `lattice_in_same_ambient_space(L::ZZLat, B::MatElem)` – reuse ambient quadratic space without isometry.
- [ ] `lattice(V::QuadSpaceWithIsom, B::MatElem)` – ensure explicit mention for ambient context reuse.

Annotate each item once wrapped or verified in Sage.
