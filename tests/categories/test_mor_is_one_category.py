r"""The exhaustive ``Mor`` invariant lives in the constructor-obligations sweep.

``tests/test_constructors_meet_their_obligations.py`` owns the single construction
registry required by ``AGENTS.md`` and checks the canonical ``A.Mor(A)`` parent for
every owned construction path.  This module intentionally contains no sampled
specimen list.
"""
